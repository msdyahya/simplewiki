// ============================================
// the ghost Security - التكوين السري
// لا تقم بتعديل هذا الملف!
// ============================================

const MSD_SECURITY = {
    // التجزئة الأساسية (سيتم توليدها تلقائياً)
    siteHash: '',
    
    // تاريخ آخر تحديث
    lastUpdate: new Date().toISOString(),
    
    // قائمة بالكود المسموح به (مشفر)
    allowedTokens: [],
    
    // وقت الجلسة (بالمللي ثانية)
    sessionTimeout: 3600000, // ساعة واحدة
    
    // عدد المحاولات الفاشلة قبل القفل
    maxAttempts: 3,
    
    // وقت القفل (بالمللي ثانية)
    lockDuration: 900000, // 15 دقيقة
};
